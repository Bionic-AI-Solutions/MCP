# Kong IngressRoute for Calculator MCP Server
# Supports both HTTP (/mcp) and SSE (/sse) protocols

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mcp-calculator-ingress
  namespace: mcp
  annotations:
    konghq.com/strip-path: "false"
    konghq.com/preserve-host: "true"
    konghq.com/protocols: "https"
    konghq.com/plugins: "mcp-calculator-cors,mcp-calculator-rate-limit,mcp-calculator-path-rewrite"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  ingressClassName: kong
  tls:
    - hosts:
        - mcp.baisoln.com
      secretName: mcp-baisoln-com-tls
  rules:
    - host: mcp.baisoln.com
      http:
        paths:
          # Main MCP endpoint - HTTP protocol
          - path: /calculator/mcp
            pathType: Prefix
            backend:
              service:
                name: mcp-calculator-server
                port:
                  number: 8000
          # SSE endpoint - Server-Sent Events protocol
          - path: /calculator/sse
            pathType: Exact
            backend:
              service:
                name: mcp-calculator-server
                port:
                  number: 8000
          # Discovery endpoint - handles POST to /calculator/ (for Cursor discovery)
          - path: /calculator/
            pathType: Exact
            backend:
              service:
                name: mcp-calculator-server
                port:
                  number: 8000
          # Messages endpoint - handles /calculator/messages requests
          - path: /calculator/messages
            pathType: Prefix
            backend:
              service:
                name: mcp-calculator-server
                port:
                  number: 8000
          # Health check endpoint (no auth required)
          - path: /calculator/health
            pathType: Exact
            backend:
              service:
                name: mcp-calculator-server
                port:
                  number: 8000

---
# CORS Plugin for Calculator
apiVersion: configuration.konghq.com/v1
kind: KongPlugin
metadata:
  name: mcp-calculator-cors
  namespace: mcp
  labels:
    app: mcp-calculator-server
config:
  origins:
    - "*"
  methods:
    - GET
    - POST
    - OPTIONS
  headers:
    - Accept
    - Content-Type
    - Authorization
    - X-API-Key
    - Mcp-Session-Id
  exposed_headers:
    - Content-Type
    - Content-Length
  credentials: true
  max_age: 3600
plugin: cors

---
# Rate Limiting Plugin for Calculator
apiVersion: configuration.konghq.com/v1
kind: KongPlugin
metadata:
  name: mcp-calculator-rate-limit
  namespace: mcp
  labels:
    app: mcp-calculator-server
config:
  minute: 1000
  hour: 10000
  day: 100000
  limit_by: ip
  policy: local
plugin: rate-limiting

---
# Path Rewrite Plugin for Calculator - Rewrites /calculator/mcp to /mcp
# Also ensures Accept header is set for FastMCP compatibility
apiVersion: configuration.konghq.com/v1
kind: KongPlugin
metadata:
  name: mcp-calculator-path-rewrite
  namespace: mcp
  labels:
    app: mcp-calculator-server
config:
  replace:
    uri: "/mcp"
  add:
    headers:
      - "Accept:application/json, text/event-stream"
plugin: request-transformer

