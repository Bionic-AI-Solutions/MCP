# Kong IngressRoute for Redis MCP Server
# Supports both HTTP (/mcp) and SSE (/sse) protocols

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mcp-redis-ingress
  namespace: mcp
  annotations:
    konghq.com/strip-path: "false"
    konghq.com/preserve-host: "true"
    konghq.com/protocols: "https"
    konghq.com/plugins: "mcp-redis-cors,mcp-redis-rate-limit,mcp-redis-path-rewrite"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  ingressClassName: kong
  tls:
    - hosts:
        - mcp.baisoln.com
      secretName: mcp-baisoln-com-tls
  rules:
    - host: mcp.baisoln.com
      http:
        paths:
          # Main MCP endpoint - HTTP protocol
          - path: /redis/mcp
            pathType: Exact
            backend:
              service:
                name: mcp-redis-server
                port:
                  number: 8010
          # SSE endpoint - Server-Sent Events protocol
          - path: /redis/sse
            pathType: Exact
            backend:
              service:
                name: mcp-redis-server
                port:
                  number: 8010
          # Discovery endpoint - handles POST to /redis/ (for Cursor discovery)
          - path: /redis/
            pathType: Exact
            backend:
              service:
                name: mcp-redis-server
                port:
                  number: 8010
          # Messages endpoint - handles /redis/messages requests
          - path: /redis/messages
            pathType: Prefix
            backend:
              service:
                name: mcp-redis-server
                port:
                  number: 8010
          # Health check endpoint (no auth required)
          - path: /redis/health
            pathType: Exact
            backend:
              service:
                name: mcp-redis-server
                port:
                  number: 8010

---
# CORS Plugin for Redis
apiVersion: configuration.konghq.com/v1
kind: KongPlugin
metadata:
  name: mcp-redis-cors
  namespace: mcp
  labels:
    app: mcp-redis-server
config:
  origins:
    - "*"
  methods:
    - GET
    - POST
    - OPTIONS
  headers:
    - Accept
    - Content-Type
    - Authorization
    - X-API-Key
    - Mcp-Session-Id
  exposed_headers:
    - Content-Type
    - Content-Length
  credentials: true
  max_age: 3600
plugin: cors

---
# Rate Limiting Plugin for Redis
apiVersion: configuration.konghq.com/v1
kind: KongPlugin
metadata:
  name: mcp-redis-rate-limit
  namespace: mcp
  labels:
    app: mcp-redis-server
config:
  minute: 1000
  hour: 10000
  day: 100000
  limit_by: ip
  policy: local
plugin: rate-limiting

---
# Path Rewrite Plugin for Redis - Rewrites /redis/mcp to /mcp
# Also ensures Accept header is set for FastMCP compatibility
apiVersion: configuration.konghq.com/v1
kind: KongPlugin
metadata:
  name: mcp-redis-path-rewrite
  namespace: mcp
  labels:
    app: mcp-redis-server
config:
  replace:
    uri: "/mcp"
  add:
    headers:
      - "Accept:application/json, text/event-stream"
plugin: request-transformer
