# Kong IngressRoute for MeiliSearch MCP Server
# Supports both HTTP (/mcp) and SSE (/sse) protocols

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mcp-meilisearch-ingress
  namespace: mcp
  annotations:
    konghq.com/strip-path: "false"
    konghq.com/preserve-host: "true"
    konghq.com/protocols: "https"
    konghq.com/plugins: "mcp-meilisearch-cors,mcp-meilisearch-rate-limit,mcp-meilisearch-path-rewrite"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  ingressClassName: kong
  tls:
    - hosts:
        - mcp.baisoln.com
      secretName: mcp-baisoln-com-tls
  rules:
    - host: mcp.baisoln.com
      http:
        paths:
          # Main MCP endpoint - HTTP protocol
          - path: /meilisearch/mcp
            pathType: Exact
            backend:
              service:
                name: mcp-meilisearch-server
                port:
                  number: 8007
          # SSE endpoint - Server-Sent Events protocol
          - path: /meilisearch/sse
            pathType: Exact
            backend:
              service:
                name: mcp-meilisearch-server
                port:
                  number: 8007
          # Discovery endpoint - handles POST to /meilisearch/ (for Cursor discovery)
          - path: /meilisearch/
            pathType: Exact
            backend:
              service:
                name: mcp-meilisearch-server
                port:
                  number: 8007
          # Messages endpoint - handles /meilisearch/messages requests
          - path: /meilisearch/messages
            pathType: Prefix
            backend:
              service:
                name: mcp-meilisearch-server
                port:
                  number: 8007
          # Health check endpoint (no auth required)
          - path: /meilisearch/health
            pathType: Exact
            backend:
              service:
                name: mcp-meilisearch-server
                port:
                  number: 8007

---
# CORS Plugin for MeiliSearch
apiVersion: configuration.konghq.com/v1
kind: KongPlugin
metadata:
  name: mcp-meilisearch-cors
  namespace: mcp
  labels:
    app: mcp-meilisearch-server
config:
  origins:
    - "*"
  methods:
    - GET
    - POST
    - OPTIONS
  headers:
    - Accept
    - Content-Type
    - Authorization
    - X-API-Key
    - Mcp-Session-Id
  exposed_headers:
    - Content-Type
    - Content-Length
  credentials: true
  max_age: 3600
plugin: cors

---
# Rate Limiting Plugin for MeiliSearch
apiVersion: configuration.konghq.com/v1
kind: KongPlugin
metadata:
  name: mcp-meilisearch-rate-limit
  namespace: mcp
  labels:
    app: mcp-meilisearch-server
config:
  minute: 1000
  hour: 10000
  day: 100000
  limit_by: ip
  policy: local
plugin: rate-limiting

---
# Path Rewrite Plugin for MeiliSearch - Rewrites /meilisearch/mcp to /mcp
# Also ensures Accept header is set for FastMCP compatibility
apiVersion: configuration.konghq.com/v1
kind: KongPlugin
metadata:
  name: mcp-meilisearch-path-rewrite
  namespace: mcp
  labels:
    app: mcp-meilisearch-server
config:
  replace:
    uri: "/mcp"
  add:
    headers:
      - "Accept:application/json, text/event-stream"
plugin: request-transformer



