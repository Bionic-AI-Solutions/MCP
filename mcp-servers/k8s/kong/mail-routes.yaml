# Kong IngressRoute for Mail MCP Server
# Supports both HTTP (/mcp) and SSE (/sse) protocols

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mcp-mail-ingress
  namespace: mcp
  annotations:
    konghq.com/strip-path: "false"
    konghq.com/preserve-host: "true"
    konghq.com/protocols: "https"
    konghq.com/plugins: "mcp-mail-cors,mcp-mail-rate-limit,mcp-mail-path-rewrite"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  ingressClassName: kong
  tls:
    - hosts:
        - mcp.baisoln.com
      secretName: mcp-baisoln-com-tls
  rules:
    - host: mcp.baisoln.com
      http:
        paths:
          # Main MCP endpoint - HTTP protocol
          - path: /mail/mcp
            pathType: Exact
            backend:
              service:
                name: mcp-mail-server
                port:
                  number: 8005
          # SSE endpoint - Server-Sent Events protocol
          - path: /mail/sse
            pathType: Exact
            backend:
              service:
                name: mcp-mail-server
                port:
                  number: 8005
          # Discovery endpoint - handles POST to /mail/ (for Cursor discovery)
          - path: /mail/
            pathType: Exact
            backend:
              service:
                name: mcp-mail-server
                port:
                  number: 8005
          # Messages endpoint - handles /mail/messages requests
          - path: /mail/messages
            pathType: Prefix
            backend:
              service:
                name: mcp-mail-server
                port:
                  number: 8005
          # Health check endpoint (no auth required)
          - path: /mail/health
            pathType: Exact
            backend:
              service:
                name: mcp-mail-server
                port:
                  number: 8005

---
# CORS Plugin for Mail
apiVersion: configuration.konghq.com/v1
kind: KongPlugin
metadata:
  name: mcp-mail-cors
  namespace: mcp
  labels:
    app: mcp-mail-server
config:
  origins:
    - "*"
  methods:
    - GET
    - POST
    - OPTIONS
  headers:
    - Accept
    - Content-Type
    - Authorization
    - X-API-Key
    - Mcp-Session-Id
  exposed_headers:
    - Content-Type
    - Content-Length
  credentials: true
  max_age: 3600
plugin: cors

---
# Rate Limiting Plugin for Mail
apiVersion: configuration.konghq.com/v1
kind: KongPlugin
metadata:
  name: mcp-mail-rate-limit
  namespace: mcp
  labels:
    app: mcp-mail-server
config:
  minute: 1000
  hour: 10000
  day: 100000
  limit_by: ip
  policy: local
plugin: rate-limiting

---
# Path Rewrite Plugin for Mail - Rewrites /mail/mcp to /mcp
# Also ensures Accept header is set for FastMCP compatibility
apiVersion: configuration.konghq.com/v1
kind: KongPlugin
metadata:
  name: mcp-mail-path-rewrite
  namespace: mcp
  labels:
    app: mcp-mail-server
config:
  replace:
    uri: "/mcp"
  add:
    headers:
      - "Accept:application/json, text/event-stream"
plugin: request-transformer

---

