# Kong IngressRoute for GenImage MCP Server
# Supports both HTTP (/mcp) and SSE (/sse) protocols

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mcp-genimage-ingress
  namespace: mcp
  annotations:
    konghq.com/strip-path: "false"
    konghq.com/preserve-host: "true"
    konghq.com/protocols: "https"
    konghq.com/plugins: "mcp-genimage-cors,mcp-genimage-rate-limit,mcp-genimage-path-rewrite"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  ingressClassName: kong
  tls:
    - hosts:
        - mcp.baisoln.com
      secretName: mcp-baisoln-com-tls
  rules:
    - host: mcp.baisoln.com
      http:
        paths:
          # Main MCP endpoint - HTTP protocol
          - path: /genimage/mcp
            pathType: Exact
            backend:
              service:
                name: mcp-genimage-server
                port:
                  number: 8008
          # SSE endpoint - Server-Sent Events protocol
          - path: /genimage/sse
            pathType: Exact
            backend:
              service:
                name: mcp-genimage-server
                port:
                  number: 8008
          # Discovery endpoint - handles POST to /genimage/ (for Cursor discovery)
          - path: /genimage/
            pathType: Exact
            backend:
              service:
                name: mcp-genimage-server
                port:
                  number: 8008
          # Messages endpoint - handles /genimage/messages requests
          - path: /genimage/messages
            pathType: Prefix
            backend:
              service:
                name: mcp-genimage-server
                port:
                  number: 8008
          # Health check endpoint (no auth required)
          - path: /genimage/health
            pathType: Exact
            backend:
              service:
                name: mcp-genimage-server
                port:
                  number: 8008

---
# CORS Plugin for GenImage
apiVersion: configuration.konghq.com/v1
kind: KongPlugin
metadata:
  name: mcp-genimage-cors
  namespace: mcp
  labels:
    app: mcp-genimage-server
config:
  origins:
    - "*"
  methods:
    - GET
    - POST
    - OPTIONS
  headers:
    - Accept
    - Content-Type
    - Authorization
    - X-API-Key
    - Mcp-Session-Id
  exposed_headers:
    - Content-Type
    - Content-Length
  credentials: true
  max_age: 3600
plugin: cors

---
# Rate Limiting Plugin for GenImage
apiVersion: configuration.konghq.com/v1
kind: KongPlugin
metadata:
  name: mcp-genimage-rate-limit
  namespace: mcp
  labels:
    app: mcp-genimage-server
config:
  minute: 1000
  hour: 10000
  day: 100000
  limit_by: ip
  policy: local
plugin: rate-limiting

---
# Path Rewrite Plugin for GenImage - Rewrites /genimage/mcp to /mcp
# Also ensures Accept header is set for FastMCP compatibility
apiVersion: configuration.konghq.com/v1
kind: KongPlugin
metadata:
  name: mcp-genimage-path-rewrite
  namespace: mcp
  labels:
    app: mcp-genimage-server
config:
  replace:
    uri: "/mcp"
  add:
    headers:
      - "Accept:application/json, text/event-stream"
plugin: request-transformer
