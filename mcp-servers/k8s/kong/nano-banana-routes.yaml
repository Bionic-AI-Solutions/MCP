# Kong IngressRoute for Nano Banana MCP Server
# Supports both HTTP (/mcp) and SSE (/sse) protocols

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mcp-nano-banana-ingress
  namespace: mcp
  annotations:
    konghq.com/strip-path: "false"
    konghq.com/preserve-host: "true"
    konghq.com/protocols: "https"
    konghq.com/plugins: "mcp-nano-banana-cors,mcp-nano-banana-rate-limit,mcp-nano-banana-path-rewrite"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  ingressClassName: kong
  tls:
    - hosts:
        - mcp.baisoln.com
      secretName: mcp-baisoln-com-tls
  rules:
    - host: mcp.baisoln.com
      http:
        paths:
          # Main MCP endpoint - HTTP protocol
          - path: /nano-banana/mcp
            pathType: Exact
            backend:
              service:
                name: mcp-nano-banana-server
                port:
                  number: 8008
          # SSE endpoint - Server-Sent Events protocol
          - path: /nano-banana/sse
            pathType: Exact
            backend:
              service:
                name: mcp-nano-banana-server
                port:
                  number: 8008
          # Discovery endpoint - handles POST to /nano-banana/ (for Cursor discovery)
          - path: /nano-banana/
            pathType: Exact
            backend:
              service:
                name: mcp-nano-banana-server
                port:
                  number: 8008
          # Messages endpoint - handles /nano-banana/messages requests
          - path: /nano-banana/messages
            pathType: Prefix
            backend:
              service:
                name: mcp-nano-banana-server
                port:
                  number: 8008
          # Health check endpoint (no auth required)
          - path: /nano-banana/health
            pathType: Exact
            backend:
              service:
                name: mcp-nano-banana-server
                port:
                  number: 8008

---
# CORS Plugin for Nano Banana
apiVersion: configuration.konghq.com/v1
kind: KongPlugin
metadata:
  name: mcp-nano-banana-cors
  namespace: mcp
  labels:
    app: mcp-nano-banana-server
config:
  origins:
    - "*"
  methods:
    - GET
    - POST
    - OPTIONS
  headers:
    - Accept
    - Content-Type
    - Authorization
    - X-API-Key
    - Mcp-Session-Id
  exposed_headers:
    - Content-Type
    - Content-Length
  credentials: true
  max_age: 3600
plugin: cors

---
# Rate Limiting Plugin for Nano Banana
apiVersion: configuration.konghq.com/v1
kind: KongPlugin
metadata:
  name: mcp-nano-banana-rate-limit
  namespace: mcp
  labels:
    app: mcp-nano-banana-server
config:
  minute: 1000
  hour: 10000
  day: 100000
  limit_by: ip
  policy: local
plugin: rate-limiting

---
# Path Rewrite Plugin for Nano Banana - Rewrites /nano-banana/mcp to /mcp
# Also ensures Accept header is set for FastMCP compatibility
apiVersion: configuration.konghq.com/v1
kind: KongPlugin
metadata:
  name: mcp-nano-banana-path-rewrite
  namespace: mcp
  labels:
    app: mcp-nano-banana-server
config:
  replace:
    uri: "/mcp"
  add:
    headers:
      - "Accept:application/json, text/event-stream"
plugin: request-transformer


